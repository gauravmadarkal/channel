import { StyleSheet } from 'react-native';

const style = {
	viewer: {
		justifyContent: "center",
		alignItems: 'center',
		margin: 10
	},
	viewerName: {
		fontWeight: 'bold',
		color: 'white'
	},
	viewer_image: {
		width: 40,
		height: 40
	}
};

export default StyleSheet.create(style);