import { StyleSheet } from 'react-native';

const style = {
	profileIcon: {
		borderColor: 'gray',
		borderWidth: 1,
		borderRadius: 50,
		padding: 10,
		marginTop: 20,
		justifyContent: 'center',
		alignItems: 'center',
	}
};

export default StyleSheet.create(style);